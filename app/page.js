import React from "react";

import "../scss/style.scss"; // Global styles

// Store initialization with wrapper
// const { store, persistor } = createStore();

const Home = () => {
  return <div>Hello </div>;
};

export default Home;
